{
  "framework": "vite",
  "buildCommand": "node scripts/validate-production-env.mjs && pnpm build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
